package be.dhs.persistence.repo.abs;

import be.dhs.persistence.abs.AbstractCreator;
import be.dhs.persistence.repo.CreatorRepo;

/**
 * Proxy interface for {@link CreatorRepo}.
 * @author	GallifreyanCode
 * @version	0.0.1-alpha
 */
public interface AbstractCreatorRepo extends CreatorRepo<AbstractCreator> {
	
}
