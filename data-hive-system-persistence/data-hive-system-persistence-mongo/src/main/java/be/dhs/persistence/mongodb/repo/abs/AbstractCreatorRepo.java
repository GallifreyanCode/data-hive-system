package be.dhs.persistence.mongodb.repo.abs;

import be.dhs.persistence.mongodb.abs.AbstractCreator;
import be.dhs.persistence.mongodb.repo.CreatorRepo;

public interface AbstractCreatorRepo extends CreatorRepo<AbstractCreator> {
	
}